"use client";

// Re-export from the provider to maintain backward compatibility with existing imports
export { useAuthModal } from "./AuthModalProvider";
export type { AuthContext } from "./AuthModalProvider";
